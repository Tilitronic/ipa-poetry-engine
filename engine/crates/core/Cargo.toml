[package]
name        = "core"
version     = "0.1.0"
edition     = "2021"
description = "Phonetic poetry analysis engine — core library"
license     = "AGPL-3.0-or-later"

[dependencies]
ndarray    = { workspace = true }
serde      = { workspace = true }
serde_json = { workspace = true }
schemars   = { workspace = true }

[dev-dependencies]
approx = "0.5"
